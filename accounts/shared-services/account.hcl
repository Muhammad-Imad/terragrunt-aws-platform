# Account-level context for the shared-services account.
# `include`d transitively via find_in_parent_folders in root.hcl.
locals {
  account_name = "shared-services"
  account_id   = "111111111111"
}
